You are executing step 5 of the Earthworks port (Path B+) in c:\dev\git\os\DiligentEngine, branch port_effort_3: TERRAFECTORS + ROADS BAKE. Steps 1-4 are done. This is the developer's #1 problem area from the previous port (terrafectors broken on Vulkan, tiles-sent-to-y=0) - the catalog localized both mechanisms; your job is a port that cannot repeat them.

READ FIRST, in this order:
1. port_effort_3\task.md S0 (NEVER commit/push; developer compiles; zero warnings; no subagents)
2. port_effort_3\execution\plan.md - "Step 5 spec"; check whether the developer recorded a BINDLESS decision in the spec/step log; update the step log when done
3. port_effort_3\execution\bringup_notes.md P1-P11+ (P4 matrix conventions, P5 readback age-gating, P10 borrowed rasterizer states)
4. port_effort_3\catalog\roads_terrafectors.md - THE authority. Non-negotiables: terrafectors NEVER render in 3D - top-down rasterization into the 8-MRT tile-bake FBO at split time, DRAW ORDER IS THE PRIORITY SYSTEM (bakeLow -> road bakeOnly -> bakeHigh -> mesh LOD6/4/2 -> overlay -> road LOD bins -> stamps -> _top combiners); the RT0 elevation blend trick (R32Float, One/OneMinusSrcAlpha, shader alpha selects additive/feathered-REPLACE/no-op - roads flatten terrain purely through this); bezierLayer bit contracts in 4 places (MACROS are authoritative, shader header comments are stale); cubicDouble UV-in-.w packing (u +- dU/3 for linear arc-length UV, left side backwards + isFlipped); spline draws = drawIndexedInstanced(64*6, numLayers) over the shared 128-quad IB; mesh terrafectors = NON-indexed drawInstanced(128*3, numBlocks) with degenerate index-0 padding; triVertex exactly 32 B; TF_material is a 464-BYTE BYTE-CONTRACT shared by cbuffer + cereal + raw fwrite (paddings are file format AND GPU layout - static_assert the offsets); road/stamp LOD binning on fixed grids (don't replace with CPU culling); bake camera = hand-built top-down V * glm::orthoLH scaled x256/248 with an IMPLICIT TRANSPOSE in the original's glm->rmcv element copy (get this wrong = empty bake; write a PORT-REVIEW comment showing your convention derivation, cf. P4).
5. port_effort_3\catalog\gpu_tile_pipeline.md (splitRenderTopdown insertion point: after bicubic, BEFORE the height_Array publish - the STEP3-STUB marker in terrain.cpp names it) + shader_interface.md (per-pass contracts).

THE Y=0 CHECKLIST (from roads_terrafectors.md S3 - apply from day one, this is why the step exists):
a) Per-RT blend translation test: RT0 must get One/OneMinusSrcAlpha while other RTs get their own modes - verify the ew::pixelShader path supports INDEPENDENT per-RT blend and that the PSO desc sets IndependentBlendEnable; log the effective blend desc per RT once at PSO build (debug).
b) TF_material 464-byte contract: static_assert sizeof + key member offsets against the shader layout.
c) Bake-camera transpose test: render one debug stamp and readback/inspect a tile's elevation around it before declaring the pass working (a zeroed/replace-blended footprint = exactly the old y=0 bug).
d) A zeroed TF_material record or missing textures must be LOUD (requireFile pattern), never a silent flat stamp.

BINDLESS DECISION POINT: the original dev wanted bindless materials; VK suffered descriptor pressure from the 4096-entry texture arrays (legacy F13/F15/F18). DEFAULT: faithful port first (descriptor arrays exactly as the extract, ew::setTextureArray) so behavior can be compared, with the array-size actually used (not blindly 4096) and unset elements bound to a dummy (VK UB otherwise). Implement bindless ONLY if the developer's step-5 launch message says so; otherwise leave a // STEP5-NOTE at the binding site describing the bindless upgrade path (Diligent runtime-sized descriptor arrays, mutable variables).

SOURCES: port_effort_3\source_extract_3\earthworks_scene\terrafector.cpp/.h, roadNetwork.cpp/.h, roads_road/bezier/Intersection/cubicDouble/materials/physics.* + hlsl\terrain\render_spline.hlsl, render_splineTerrafector.hlsl, render_meshTerrafector.hlsl, render_ribbons.hlsl. Structure-preserving; roads editor GUI is stripped (editor-deferred) but ALL load/bake/render logic ports; roads_physics ports as-is (future consumers; note: ODE_bezier::bezierBounding is populated nowhere even in the original - port faithfully, add a PORT-REVIEW note, do not "fix"). lodTriangleMesh FBX import goes through Assimp exactly as the extract (OTSCommon vendored). Legacy = techniques reference only. DiligentCore readable - verify everything.

SCOPE - IN: full terrafector system (materialCache, terrafectorEditorMaterial load/bake path, lodTriangleMesh, sb_Terrafector_Materials, exportMaterialBinary), road network load (.roadnetwork cereal - external version #define 103, files embed NO version, upgrade() flow per conventions_app_wiring.md S8), road geometry -> GPU (updateAllRoads/updateDynamicRoad/convertToGPU), splitRenderTopdown reinserted at its STEP3-STUB position with the full priority-ordered bake, terrain.cpp load-path wiring (lastfile.road etc.), Sprites.cpp/.h port IF road code paths reference mSpriteRenderer (they do for selection markers - port the sprite system minimally or stub the marker calls with STEP5-STUB, your judgment, document it).
SCOPE - OUT: road EDITING interactions (selection/vertex edit - editor-deferred; the mouse-driven code paths in terrain.cpp that call doSelect etc. stay stubbed; eventually there will be editors to pick this up, but it isn't our focus now), buildings (step 6), AI (removed by decision).

CRITICAL RULES: extract = authority; structure-preserving; // PORT-REVIEW deviations, // STEP5-STUB deferrals; padding/byte-contracts load-bearing; both APIs; zero warnings; cannot compile - verify against real headers; update plan.md step log + bringup_notes.md (new P entries - especially anything Vulkan-blend/format related, the old port died here).

RETURN: files created/modified; scope covered vs deferred; how each y=0 checklist item is enforced in code; bindless decision taken (or deferred with note); the developer's verification sequence (which terrain features to look at: roads visible in terrain, flattened road beds, stamp materials) and the debug toggles to bisect a broken bake (bake stages on/off); new risks.
