You are a code analyst for Port Effort 3 in c:\dev\git\os\DiligentEngine (branch port_effort_3). Read port_effort_3\task.md first (S0 ground rules, S1 goal, S5 Concept Catalog). NEVER run any git state-changing command (no commit/add/checkout). Write ONLY your deliverable file. Never modify source files. Do NOT spawn subagents - do the work yourself.

DELIVERABLE: port_effort_3\catalog\conventions_app_wiring.md

SCOPE - the cross-cutting doc:
1. World/engine conventions: coordinate system (Y-up, RH, NORTH=-Z EAST=+X, terrain +-20000m), matrix conventions (glm, handedness, depth [0,1], Falcor 24mm film-back FOV), units, time. Source: extract code + EarthworksFX\PROJECT_OVERVIEW.md + BRINGUP_NOTES.md (extract the F-findings that are ORIGINAL-code semantics rather than compat-layer bugs, cite F-numbers).
2. App shell wiring (source_extract_3\Earthworks_4.cpp/.h): onLoad order (graphics state, camera init, atmosphere->terrain texture wiring, camera.bin persistence, presets cereal, tonemapper+colorCube 33^3 LUT, shadowEdges thread startup), onFrameUpdate->onFrameRender flow, shaderLightBuffer contents, hdrFbo formats (R11G11B10 + D24S8), hdrPreviousFrame half-res blit feedback, refresh.minimal power mode. Map each element to its equivalent in the current port's app shell (EarthworksFX\src\app\EarthworksFXApplicationBase.* + EarthworksFX\src\core\Earthworks_4.cpp) - the fresh port keeps that Diligent shell, so document what must be re-wired.
3. Shader/compute wrappers (computeShader.*, pixelShader.* - tiny): exact semantics (load, defines, Vars/State, drawInstanced/drawIndirect/dispatch signatures).
4. File formats & persistence: everything serialized/loaded - lastFile.xml, earthworks4_presets.xml, camera.bin, cereal archives (versions! e.g. ROADNETWORK_CEREAL_VERSION 103, CEREAL_CLASS_VERSION tags), terrain data directory layout (elevation/root4096.bil, jp2 tiles, colorcubes, plant binaries, ecotopes), binary import/export formats. Version-compatibility matters: existing terrain data (assets/terrains/switserland_Steg) must load in the new port.
5. Global Falcor API inventory: enumerate the complete Falcor API surface used across source_extract_3 (classes, statics, methods - grep-based, deduplicated, with usage counts) -> THE key input for deciding compat-shim vs direct-Diligent per subsystem. Compare against what the previous port's shim implemented (EarthworksFX\interface\Falcor.h).
6. Threading map: every std::thread/detach/mutex/atomic in the extract (shadowEdges solveThread, streaming workers, terrainGenerator async) - ownership and synchronization patterns.

RETURN: ~15-line summary incl. Falcor-API-surface size (distinct types/methods) and the riskiest convention traps.

CONTEXT SOURCES (read-only): current port EarthworksFX\src\core\ + EarthworksFX\hlsl\ (a previous port of the same code - drift reference), EarthworksFX\BRINGUP_NOTES.md + EarthworksFX\PROJECT_OVERVIEW.md (mine relevant F-findings; do not re-derive). The extract was stripped of editor GUI/glider/cfd - '// STRIP-REVIEW:' tags mark deliberate keeps; pristine original at C:\dev\git\os\Earthworks_4-F5_2\ (STRICTLY READ-ONLY) if you need stripped context.

DOC TEMPLATE (use these sections):
1. Purpose & data flow (CPU/GPU, per-frame vs async)
2. Core tricks & clever mechanisms - the things a rewriter MUST NOT lose; each with file:line refs and WHY it matters
3. Invariants & conventions
4. Performance-critical details - why this is fast; what change would silently kill perf or visuals
5. GPU resources & shader interface (buffers/textures/cbuffers incl. layout+PADDING - a past porting agent deleted 'unused' byte padding and broke rendering, padding is load-bearing)
6. Dependencies: what it consumes/provides to other subsystems
7. Falcor API surface actually used (types/calls) - input for the port-path decision
8. Port drift notes: how EarthworksFX's version differs, relevant BRINGUP_NOTES F-findings, known bugs
9. Bad code / removable candidates (clearly labeled as opinion, never act on it)
10. Open questions / uncertainties (flag, don't guess)

Audience: a cold-context agent re-porting this to native Diligent + a reviewer checking nothing hyper-tuned was lost. Precision beats prose; tables and file:line citations. RETURN: a ~15-line summary of the most important tricks/invariants you found.
