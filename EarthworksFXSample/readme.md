# EarthworksFX sample

Derived from the Atmosphere sample, we're simply trying to render a default terrain from the ENV var ACSMP_GAMEROOT\terrains\switserland_Steg.

Atm I'm using this for the terrain code port, but in the end it could be the demo/test for any feature that is inside the DiligentEngine as a library/toolset.